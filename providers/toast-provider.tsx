"use client";

import { Toaster } from "react-hot-toast";

// Este proveedor se encarga de renderizar el componente Toaster de react-hot-toast en la aplicación para mostrar notificaciones al usuario.
export const ToastProvider = () => {
  return <Toaster />;
};
